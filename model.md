# Model을 알고 Agent를 알아라

코딩 에이전트를 직접 만들어 보면 한 가지를 깨닫게 된다.

**모델이 학습되지 않은 방향으로 도구를 정의하면, 그 도구는 깨진다.**

아무리 멋진 tool schema를 짜고, 아무리 정교한 system prompt를 써도, 모델이 본 적 없는 형식이면 따라오지 못한다. 이 글은 로컬 LLM 기반 코딩 에이전트(`kanna`, `smoker`)를 만들면서 부딪힌 문제들과, 그 과정에서 정리된 생각들을 적은 글이다.

---
 
## 1. Edit Tool, 한 쉽지 않다
 
코딩 에이전트의 핵심 도구 중 가장 까다로운 게 **파일 수정 도구(edit tool)** 다. "이 파일을 이렇게 고쳐"가 표면적으로는 단순해 보이지만, 구현에 들어가면 함정이 한가득이다.
 
### 전체 파일 재작성? 토큰 낭비
 
가장 단순한 방법은 모델에게 수정된 파일 전체를 출력하게 하는 것이다. 30줄짜리 파일이면 모르겠는데, 500줄짜리 파일에서 한 줄 고치려고 매번 500줄을 다시 뱉게 하면 토큰 낭비도 심하고 속도도 느리다. 게다가 긴 출력을 베껴 쓰다가 다른 줄을 무심코 망가뜨릴 위험까지 있다. 거의 모든 모던 에이전트에서 버려진 방법이다.
 
### Git diff를 만들게 한다? 로컬 LLM에는 절대 안 된다
 
그럼 git diff를 만들게 해서 적용하면 되지 않을까?
 
```diff
@@ -42,3 +42,3 @@
-    val x = 1
+    val x = 2
```
 
이론적으론 가장 깔끔하다. 그런데 **로컬 LLM(7B~35B급)한테 이걸 시키면 거의 항상 깨진다.** 이유는 단순하다.
 
**LLM은 라인 번호를 세지 못한다.**
 
지능이 있어 보여도, LLM에게 "이 파일의 42번째 줄"이라는 개념은 부정확하다. Tokenizer가 줄 단위로 동작하지 않기 때문에 hunk header(`@@ -42,3 +42,3 @@`)의 줄 번호는 거의 항상 어긋난다. line offset이 한두 줄만 틀어져도 patch는 reject되고, 에이전트는 재시도 루프에 빠진다. **모델의 학습 분포 자체가 라인 단위 카운팅에 약하기 때문이고, prompt를 잘 쓴다고 해결되는 문제가 아니다.**
 
### 그럼 Codex는 어떻게 하지?
 
OpenAI Codex CLI는 오픈소스다. `apply_patch`라는 freeform tool을 쓰는데, V4A diff 형식으로 git diff와 비슷해 보이지만 **결정적인 차이**가 있다 — **라인 번호가 없다.**
 
```
*** Begin Patch
*** Update File: src/foo.kt
@@ class Foo {
   fun bar() {
-    return 1
+    return 2
   }
}
*** End Patch
```
 
라인 번호 대신 `@@` 뒤에 **컨텍스트 식별자**(클래스명, 함수명)와 주변 코드 몇 줄로 위치를 잡는다. 모델이 라인을 세지 않아도 되도록 설계된 것이다.
 
OpenAI는 GPT-4.1 cookbook을 공개하면서 잘 작동하는 edit format의 두 가지 원칙을 명시했다 — **라인 번호 회피, 그리고 원본과 교체본을 분명한 구분자로 나누기.** V4A는 이 두 원칙을 가장 직접적으로 구현한 사례다.
 
여기서 중요한 사실 — **이 V4A 포맷을 Qwen이나 Gemma한테 시키면 거의 못 만든다.** OpenAI가 GPT-4.1과 Codex 계열에 이 포맷을 집중적으로 학습시켰기 때문이고, 다른 모델은 본 적이 없는 형식이라 깨진 출력을 뱉는다.
 
### 모델마다 학습된 포맷이 다르다
 
open 소스 coding agent 중 제일 유명한 aider를 보자, 모델별로 edit format을 다르게 매핑해서 쓴다. 매핑이 실제로 코드에 박혀 있다:
 
| 모델 계열 | 잘 먹는 포맷 |
|---|---|
| GPT-4.1 / Codex | V4A Patch (`*** Begin Patch`) |
| Claude | search/replace, V4A 둘 다 |
| Gemini / Gemma | diff-fenced (SEARCH/REPLACE, 파일명이 fence 안쪽) |
| Qwen 3 / 3.5 | search/replace (general function calling 베이스) |
| Mistral / Codestral | search/replace |
 
aider가 Gemini용으로 일부러 만든 게 **diff-fenced**다. Gemini가 일반 diff 포맷에서 fence 위치(코드 블록 안/밖)를 자주 헷갈려해서, 파일명을 fence 안쪽에 넣는 변종을 만들어야 했다. 모델의 약점을 알고 포맷을 비틀어준 것이다.
 
그리고 format이 아무리 잘 잡혀 있어도 모델은 미세하게 어긋난다 — 공백 한 칸, trailing newline, 들여쓰기 한 단계. 그래서 잘 만들어진 edit tool은 모두 **fallback matching 사다리**를 쌓아 둔다.
 
- **Codex `apply_patch`**: 정확 일치 → 줄 끝 공백 무시 → 모든 공백 무시 (3단계)
- **aider**: 정확 → 공백 무시 → 들여쓰기 유지 → `difflib` fuzzy match (4단계)
- **RooCode**: Levenshtein 거리 기반 middle-out 매칭으로 best-match 점수 산출 (한 단계 더)
이 사다리 자체가 모델이 얼마나 어긋날 수 있는지를 그대로 반영한 설계다. **Format이 모델 학습 분포를 따르는 것이라면, fallback layer는 모델의 imperfection을 흡수하는 또 다른 인프라다.** 둘 다 결국 같은 원리 — 모델이 정확히 못해도 깨지지 않게 환경을 다듬어 두는 것.
 
결국 **"가장 좋은 edit format"이라는 건 없다.** 모델마다 학습된 분포가 다르고, 그 분포 안에서 잘 동작하는 포맷을 골라야 한다. 그리고 이 원칙은 edit format에만 적용되는 게 아니다.
 
---
 
## 2. 모델이 학습된 방향을 거스르지 마라
 
Edit format은 모델의 학습 분포가 드러나는 가장 눈에 띄는 표면일 뿐이다. **tool 정의 전반에 같은 원리가 깔려 있다.**
 
mistral code, qwen code, codex CLI, aider — 네 에이전트의 tool 정의를 나란히 놓고 비교해 보면 한 가지가 눈에 띈다. **거의 모든 에이전트의 tool 어휘가 같다.** `read_file`, `grep`, `glob`, `edit`, `bash`. 이름과 파라미터까지 거의 비슷하다.
 
이유가 있다. 모델들이 그 어휘로 학습됐기 때문이다. 특히 **CLI 기반 코딩 에이전트 데이터**가 학습에 대량으로 들어갔다. Qwen3-Coder, gpt-oss, Codestral 모두 grep/glob/read_file로 코드를 뒤지고 edit로 고치는 패턴에 강하게 학습돼 있다.
 
그래서 도메인 특화 tool을 새로 만들어 줘도 모델은 그걸 잘 부르지 못한다. Kanna에서 Plan Agent에게 `analyzeClass(file, category_hint)` 같은 정밀 tool을 줬을 때 정확히 이 일이 벌어졌다. **모델은 그 tool을 무시하고 자꾸 `grep`을 호출하려 들었다.** Tool 리스트에 grep이 없으면 환각으로 채웠다 — "src/foo.kt를 봤더니..." 같은 식으로. 모델이 학습한 그림으로 돌아가려는 관성이, 내가 만든 환경의 정교함보다 훨씬 강했다.
 
> **모델이 학습된 곳으로 가야 한다.**
> 거기서 거스르려는 순간, 환경이 아무리 정교해도 모델은 뒤로 돌아간다.
 
이 사실을 받아들이고 나면 tool 설계가 단순해진다.
 
- **Tool 이름은 표준 어휘 그대로** (`read_file`, `grep`, `glob`, `edit`)
- **파라미터 이름도 표준** (`old_string`, `new_string`, `path`, `pattern`)
- **새 발명을 자제**한다. 새 도구는 모델이 학습한 적이 없다.
다만 여기서 끝이 아니다. **tool이 호출되는 방식뿐 아니라, tool이 끝난 뒤 모델에게 돌려주는 결과도 학습 분포의 일부다.** 이 영역은 한 단계 더 안 보이고, 더 자주 무시된다.

---
 
## 마치며

Kanna에서 5개월 동안 gpt-oss, gemma, qwen, mistral을 바꿔가며 싸웠다. 내가 짜 놓은 판 위에서 잘 굴러갈 경량급 모델을 찾으려고. 모델을 갈아 끼우면 풀린다고 믿었다.   
지금 Smoker에서는 방향이 정반대다. 모델을 알고 agent를 안 이후로, 모델을 골라 끼우는 대신 받아들였다. 지금은 어떤 turn 설계가 경량급 모델에서도 좋은 성능을 끌어내는지를 고민하고 있다.
 
> Coding 에이전트를 만든다는 건 **새로운 프로토콜을 발명하는 일이 아니다.**
> 모델이 이미 학습한 프로토콜을 정확히 골라내고, 인프라를 거기에 맞추는 일이다.
 
- Edit tool은 모델이 학습된 **포맷**으로
- Tool 이름과 파라미터는 모델이 학습된 **표준 어휘**로
- System prompt는 모델이 학습된 **워크플로우 패턴**으로
- Tool 반환값은 모델이 다음 턴에서 **자연스럽게 받을 수 있는 형태**로
그리고 그 "모델이 학습된 것"을 알아내는 가장 좋은 방법은 — **그 모델을 만든 회사가 공개한 에이전트의 소스를 까보는 것**이다.

모델과 씨름하지 말고, 받아들이자.
